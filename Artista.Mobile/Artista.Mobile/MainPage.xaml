<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Artista.Mobile.ViewModels"
             xmlns:local="clr-namespace:Artista.Mobile"
             x:Class="Artista.Mobile.MainPage">
    <StackLayout BackgroundColor="White">
        <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
            <Label Text="{Binding Title}" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <Label Text="View amazing works of art." FontSize="Title" Padding="30,10,30,10"/>
        <Label Text="Discover treasured art collections located in museums from around the world." FontSize="16" Padding="30,10,30,10"/>
        <Image
            Source="{Binding MainArtWork.PrimaryImageSmall}"           
        />
        <ImageButton            
            HorizontalOptions="CenterAndExpand"
            Source="{local:ImageResource Artista.Mobile.images.refresh.png}"
            Command="{Binding LoadImageCommand}"
            BackgroundColor="White"
            />
        <Button
            Text="Search"
            Command="{Binding SearchCommand}"/>
    </StackLayout>

</ContentPage>
